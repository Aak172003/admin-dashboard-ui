export type Credentials = {
    email: string;
    password: string;
};


export type UserData = {
    id: number;
    firstName: string;
    lastName: string;
    email: string;
    role: string;
    createdAt: string;
};

export type TenantData = {
    id: number;
    name: string;
    address: string;
};


